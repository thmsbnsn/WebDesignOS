# Security Model

WebDesignOS relies on deterministic checks rather than manual approvals.

## Principles
- Least privilege and explicit policies.
- RLS-first database posture (no blanket allow).
- No secrets committed to the repo.

## Gate
The security gate is the source of truth for PASS/FAIL.
